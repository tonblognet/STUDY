# Handoff текущего слоя

## Продолжение 22 сентября 2026: редакционная публикация

Layer 01, та же ветка. Накопленные изменения предыдущих блоков сохранены.

- Каталог получил CatalogRevision/CatalogHead, строгую валидацию структурированных
  снимков, checksum, diff с provenance, редакторскую очередь и историю решений.
- Публикация атомарна; PostgreSQL блокирует одновременные изменения указателя.
  Повторный импорт не создаёт дубль, устаревший черновик не перезаписывает новую
  версию. Ошибка записи аудита откатывает публикацию и связанные записи.
- Публичные страницы, API, сравнение, подбор и кабинет используют одну публикацию.
  Файловый снимок доступен отдельным режимом, не fallback при недоступности БД.
- Добавлены `/admin/catalog`, `/admin/catalog/[id]`, `/admin/programs/[slug]`.
  Исправление стоимости/мест создаёт review; публикация требует отдельного решения.
- Исправлена привязка строк МГУ по позиции массива: опубликованные программы
  формируют строки сами, исходные описания сопоставляются по факультету/коду/названию.
  Места берутся из публикации и показывают собственные источники.
- Общий adapter больше не выдаёт найденный маркер за готовую проверку: он явно
  сообщает ноль извлечённых фактов и обязательный review. Универсальный scraper
  не заявляется реализованным.
- Полнота данных не изменилась: 162 организации, 185 Program, отдельные 419
  перечней и 5 групп МГАХ. Новые фактические цены/места не придумывались. Для
  конфликтного проходного балла МГУ убрана числовая проекция; provenance сохранён.
- Порядок оставшихся слоёв уточнён по исходному промпту: AI после Stage 1.
- Инструкции: `docs/CATALOG_PUBLICATION.md`, ADR 0005. `data:report` описывает
  входной снимок репозитория, а не последующие редакторские изменения в БД.
- Полный импорт выявил ошибочный unique по вузу/коду/уровню/форме: разные
  факультеты могут иметь одинаковую комбинацию. Миграция оставляет уникальный
  slug и обычный составной индекс. Тест публикует все 185 программ.
- Семь gates прошли по порядку: format, lint, typecheck, 264 unit/component
  теста (22 файла), production build, 9 SSR + 8 PostgreSQL/HTTP сценариев,
  security audit без известных production-уязвимостей. `data:validate` прошёл;
  предупреждения о неизвестных данных сохранены. Логи: `tmp/layer01-final-*.log`.
  После финальных визуальных исправлений проверки повторены. После перерыва
  тестовая PostgreSQL была остановлена: первый повтор integration упал на
  подключении, после запуска БД все 17 сценариев и audit снова прошли.
- Миграции и браузерные правки применялись только в локальной PostgreSQL 16,
  UTF-8 тестовой БД, в отдельных схемах. Production БД не менялась.
- Browser QA: `http://localhost:3100`, production build, режим database,
  редактор локальной тестовой БД. Проверены 1440×1000 и 390×844: `/`, `/programs`,
  `/programs/hse-economics`, `/universities`, `/universities/mgu`,
  `/universities/balletacademy`, `/compare`, `/match`, `/account`, `/admin`,
  `/admin/catalog`, `/admin/catalog/[id]`, `/admin/programs/hse-economics`.
  Поиск редактора, пустой результат, форма с официальным источником,
  отклонение чужого домена, diff, заблокированное подтверждение до заполнения,
  публикация и отказ проверены действиями в браузере. До одобрения заметки
  нет в публичной карточке; после одобрения она видна; отклонённая не видна.
  Все проверенные маршруты отвечают 200, горизонтального переполнения документа
  и сломанных загруженных изображений нет, pageerror не зарегистрирован.
  Снимки просмотрены; артефакты локально в `output/playwright/final-*`.
  В существующих страницах вуза и подбора остаются вложенные main — отдельная
  задача доступности слоя 03. При прерванных переходах Next записывал
  `destination stream closed early`; повторные полные переходы успешны.
- Для официальных локальных логотипов `.gitattributes` сохраняет исходные
  байты и допускает завершающие пробелы источника: SHA-256 не изменяется ради
  форматирования. Личные данные, временные БД, учётные данные QA и логи в Git
  не включены. `git diff --check` и staged diff проверены.

Фокус ревью: миграции и неизменяемость истории, транзакционная публикация,
проверка официальных доменов/точных URL, одинаковая версия на всех публичных
маршрутах, раздельная модель Program и творческих кампаний. Интерфейс правит
три числовых поля; остальные изменения вносятся через экспорт/импорт снимка.
Нет универсального извлечения из произвольных документов и фонового worker.
Полнота юридического реестра, неизвестные условия и пропуски логотипов остаются
в матрицах покрытия. Архив МГУ 2011–2025 остаётся статическим приложением.

Слой не считается слитым или одобренным. Следующие слои не начаты.

## Последнее дополнение: 17 сентября 2026, условия приёма и источники

- Layer 01, та же ветка. Ранее подготовленный индекс сохранён; коммиты, push, миграции и запись в БД не выполнялись.
- МГАХ: добавлен отдельный снимок `data-sources/moscow/admission-campaigns.json` из шести официальных документов 2026. Пять групп, 32 бюджетных места, общий пул восьми заочных платных мест, четыре тарифа. Два творческих/профессиональных экзамена сохранены отдельно. Отсутствие цены/мест остаётся `null`, нулевые значения публикуются только при явном указании. У каждого документа URL, SHA-256 и дата чтения; PDF таблицы дополнительно сверены визуально.
- Эти группы показаны в карточке и quality report, но не включены в 185 записей Program, автоматический подбор и импорт БД. Модель подбора поддерживает только одно ДВИ; изменение этой модели не относится к текущему блоку. Валидация проверяет источники, общие пулы, дубли, числовые пределы и порядок дат. Основной период приёма не представлен как открытый сейчас.
- Universal University: обновлены шесть основных полей, добавлен собственный логотип из официального SVG-спрайта и выписка от 28.04.2026. Итог каталога: 162 организации, 83 логотипа, 79 отсутствуют, 29 датированных выписок. Текущий статус реестра по-прежнему не подтверждён. 419 образовательных перечней и 1 072 исторические укрупнённые группы учитываются отдельно.
- МГУ: ещё 11 сроков из индивидуальных карточек `openday.msu.ru`, сопоставленных по факультету, уровню и профилю. Теперь 73 цены и 68 сроков из 85 групп; неизвестны 12 цен и 17 сроков. Даты обновлены только у проверенных фактов. «Лечебное дело»: необычные 7 лет в каталоге требуют сверки с учебным планом, срок оставлен неизвестным. Конфликты 322/324 и 40/42 не сняты.
- Проверки в установленном порядке: `format:check`, `lint`, `typecheck`, `test` (257 тестов / 21 файл), `build`, `test:integration` (9), `security:audit` — все прошли. Логи `tmp/next-final-*.log`. `data:validate`, `data:report` и `data:import --university=balletacademy --dry-run` также прошли; сухой импорт показывает 1 вуз и 0 Program, как предусмотрено отдельной моделью.
- Browser QA: `/universities`, `/universities/balletacademy`, `/universities/universal`, `/universities/mgu`; 1440×1000 и 390×844. Проверены поиск, переходы, открытие минимумов и профильных экзаменов, общий пул, нули и неизвестные значения, официальный источник нового срока политологии, новый логотип и дата выписки. Переполнения документа нет, изображения новых карточек загружаются, console error и server stderr пусты. Один переход из каталога потребовал прямого открытия, другой завершился с задержкой; повторные переходы в каталог и Universal прошли. Мобильные и настольные снимки просмотрены.
- Обновлены таблица покрытия МГУ и сводка каталога; `git diff --check` прошёл. Слой целиком не объявляется готовым: юридический реестр недоступен, логотипы и подробные условия большинства вузов ещё требуют источников.
- Сайт открыт на `http://localhost:3100/universities`, временный viewport сброшен. Фоновый production-сервер: PID 39460 на момент записи, `node D:\STUDY\.next\standalone\server.js`; логи `tmp/next-site-out.log`, `tmp/next-site-err.log`. Перед новой сборкой проверить команду PID и остановить именно этот процесс.

## Идентификация

- Дата смены: `2026-09-22`
- Слой: `01 — data foundation`
- Ветка: `codex/layer-01-data-foundation`
- Исполнитель: владелец проекта + Codex
- Следующий исполнитель: второй разработчик после явной передачи смены
- Статус: Layer 01 передаётся на ревью; следующий слой не начинается до одобрения и merge

## Состояние в начале смены

- `main` и `origin/main` совпадали с rebuild baseline `e0d5ce5`.
- Друг не создавал веток и не отправлял изменений.
- Публичный каталог содержал 20 вузов и 101 программу, но registry ingestion-адаптеров покрывал только 10 вузов.
- `pnpm data:validate` падал с 1 120 критическими ошибками «неофициальный URL» для второй половины каталога.
- `data-sources/reports/quality-report.json` описывал только 10 вузов и 10 программ.
- Импорт пропускал новую версию метрики, если менялись locator, документ, дата проверки, проверяющий или редакторская заметка, но значение и URL оставались прежними.

## Что реализовано

- Добавлены отдельные 2026 adapters для МГТУ им. Баумана, МГИМО, Пироговского, Сеченовского, Губкинского университетов, МГЮА, РХТУ, РГГУ, Финансового университета и Московского Политеха.
- Реестр теперь покрывает каждый вуз публичного каталога и принимает программные источники только с официального домена соответствующего вуза.
- Pipeline останавливается при пропущенном, лишнем или дублирующемся адаптере.
- Добавлен regression-тест, сопоставляющий adapters со всеми университетами и основными URL всех 101 программ.
- `pnpm data:validate` теперь проходит для всего каталога без критических ошибок; неизвестные экзамены остаются предупреждениями и не подменяются предположениями.
- Quality snapshot регенерирован: 20 вузов, 101 программа, 1 387 sourced indicators, 0 конфликтов источников.
- Версионирование импорта теперь сравнивает не только значение, статус и URL, но также документ, страницу, лист, диапазон, секцию, даты получения и проверки, редактора, срок следующей проверки и заметку.
- Добавлены тесты, подтверждающие идемпотентность полностью одинакового импорта и создание новой версии при изменении provenance.
- Добавлена кроссплатформенная настройка Prettier `endOfLine: auto`, чтобы обязательный format gate одинаково работал на Windows и Unix checkout без массовой перезаписи файлов.
- Добавлен source-aware DTO для официального сайта, логотипа, адреса, числа общежитий, ВУЦ и адресов кампусов всех 20 вузов.
- Для каждого university fact сохраняются `sourceKind`, точный URL, год, дата получения и проверки, проверяющий, locator, срок следующей проверки, заметка и quality status.
- Создана версионная Prisma-модель `UniversityFactValue` и миграция; идентичный импорт пропускается, а изменение значения, типа источника или provenance создаёт новую версию.
- В денормализованные поля `University` и `Campus` при импорте попадают только значения со статусом `verified`; `pending_review` не публикуется как установленный факт.
- Подтверждены официальными страницами адреса основного кампуса МГТУ им. Баумана, МГИМО и Сеченовского университета; для Сеченовского также зафиксирована отдельная официальная страница логотипов.
- Quality snapshot теперь отдельно считает 111 university/campus indicators и содержит их точные `factSources`: всего 1 498 индикаторов, 254 verified, 147 pending review, 1 064 not published и 0 конфликтов.

## Официальные источники нового registry

- `priem.bmstu.ru`
- `mgimo.ru/study/admission`
- `rsmu.ru/abitur/bachelor`
- `sechenov.ru/admissions`
- `postupai.gubkin.ru`
- `msal.ru/content/abiturientam`
- `chemeng2030.muctr.ru`
- `rsuh.ru/sveden/education`
- `fa.ru/for-applicants/abitur2026`
- `mospolytech.ru/upload/.../4.1_Moskva.pdf`

Точные URL, типы документов, год и locators находятся в соответствующих файлах `data-sources/universities/*/adapter.ts` и в quality snapshot.

## Проверки текущего блока

- Предварительный regression: новый adapter-тест падал на отсутствии 10 конфигураций.
- `data-sources/core/adapters.test.ts`: 23 теста прошли.
- `data-sources/core/metric-version.test.ts`: 9 тестов прошли.
- `pnpm data:validate`: 101 программа, 0 критических ошибок.
- `pnpm data:report`: snapshot успешно создан для 20 вузов.
- `pnpm format:check`: пройдено.
- `pnpm lint`: пройдено.
- `pnpm typecheck`: пройдено.
- `pnpm test`: 13 файлов, 70 тестов пройдено.
- `pnpm build`: production build пройден, сгенерировано 154 маршрута/страницы.
- `pnpm test:integration`: 4 сценария пройдено.
- `pnpm security:audit`: известных production-уязвимостей нет.

### Второй блок: university/campus provenance

- TDD regression до реализации падал из-за отсутствующего `lib/university-facts`.
- `data-sources/core/university-facts.test.ts`: 5 тестов прошли.
- `data-sources/core/metric-version.test.ts`: 10 тестов прошли, включая смену типа источника university fact.
- Целевой набор: 41 тест пройден.
- `pnpm data:validate`: 20 профилей вузов и 101 программа, 0 критических ошибок.
- `pnpm data:import --dry-run`: 101 программа и university facts прошли проверку, база не изменялась.
- `pnpm data:report`: snapshot создан для 20 вузов, 101 программы и 1 498 индикаторов.
- `pnpm format:check`: пройдено.
- `pnpm lint`: пройдено.
- `pnpm typecheck`: пройдено.
- `pnpm test`: 14 файлов, 76 тестов пройдено.
- `pnpm build`: production build пройден, сгенерировано 154 статических варианта страниц.
- `pnpm test:integration`: 4 сценария пройдено.
- `pnpm security:audit`: известных production-уязвимостей нет.
- `prisma generate --no-engine` использован, потому что DLL обычного Prisma engine был занят работающим локальным dev server; сервер пользователя не останавливался. Чистая установка генерирует стандартный клиент обычным способом.

### Третий блок: профиль МГУ, каталог и архив

- Вместо одной программы МГУ — 85 конкурсных групп московского бакалавриата/специалитета 2026. Общий каталог: 20 вузов, 185 программ.
- Добавлены официальные описания, факультеты, коды, места, испытания с альтернативами, квоты и ссылки на конкретные страницы PDF.
- Добавлены 909 архивных записей за 2011–2025 годы; обе волны старых кампаний сохраняются. Разные шкалы не превращаются в общий тренд.
- Конфликт по математике 2025 (322 в сводке, 324 у мехмата) помечен `conflicting_sources`; подбор не доверяет этой метрике. В quality report это два индикатора одного конфликта: текущий показатель и история.
- Добавлены подтверждённый адрес, контакты, ссылки на комиссии, общежитие, ВУЦ, документы и карта с загрузкой по кнопке.
- Поиск и фильтр факультета работают внутри каталога, архив имеет независимые поиск, выбор года и дозагрузку записей.
- Неподтверждённые стоимость, продолжительность, минимумы и общая целевая квота не выдуманы. Полнота карточек рассчитывается, текст «Срок уточняется» не повышает её.
- Условия покрытия, исключения, источники и воспроизведение: [MGU_DATA_COVERAGE.md](MGU_DATA_COVERAGE.md).
- Quality snapshot: 3 537 индикаторов по всему каталогу; МГУ — 2 057, из них 1 291 verified, 750 pending review, 2 индикатора конфликта.

Проверки 8 сентября:

- `pnpm data:validate`: без критических ошибок; неизвестные данные остальных вузов остаются предупреждениями.
- `pnpm data:import --dry-run`: пройдено, база не изменялась.
- `pnpm data:report`: обновлён.
- В обязательном порядке прошли format:check → lint → typecheck → test (84 теста, 16 файлов) → build (238 вариантов страниц) → test:integration (6 сценариев) → security:audit (нет известных production-уязвимостей).
- Добавлены регрессии идентичности профилей, альтернатив ЕГЭ, волн архива, конфликта источников и диапазона агрегатных баллов. Интеграция проверяет SSR профиля МГУ, карту без iframe до согласия и историю без ложного тренда.
- Браузерные проверки выполняются на production-сервере `http://127.0.0.1:3000`; подробные результаты ниже.

Браузерная проверка МГУ:

- `/universities/mgu`, desktop 1440×1000 и mobile 390×844: горизонтального переполнения страницы нет.
- Поиск «физика» + фильтр «Физический факультет» дают три группы; карточка нового теоретического профиля раскрывает испытания, квоты и источник страницы 3.
- Несуществующий запрос показывает 0 и объяснение. Поиск архива независим от поиска программ.
- Архив 2015 + «Математика»: три записи, у мехмата показаны обе волны 346/328. В 2011 проверено пустое состояние; очистка даёт 54 записи, кнопка раскрывает оставшиеся 24.
- Проверены переходы якорей, видимый фокус полей и адаптация длинных названий. Скриншоты находятся локально в `output/playwright/` (не коммитятся).
- Карта загружается после нажатия; адрес геокодирован в один объект, масштаб исправлен с регионального на окрестности Главного здания. Проверена резервная внешняя ссылка.
- До внешней карты ошибок/предупреждений консоли приложения не обнаружено. Сам Яндекс иногда блокирует собственное websocket-подключение метрики по своей CSP; это не блокирует отображение карты, CSP приложения ради метрики не ослаблена.
- Исправлена обнаруженная ранняя загрузка логотипа до hydration: mount-ref учитывает уже завершённое изображение вместо зависшей буквенной заглушки.
- Интеграционный сервер теперь запускается напрямую через Node, без Windows shell-обёртки, чтобы завершение теста не оставляло дочерний сервер со старой сборкой.

## Следующий конкретный шаг Layer 01

Продолжить сверку оставшихся 12 цен и 17 сроков МГУ, разрешить конфликт математики, проверить центральную публикацию результатов 2026. Минимумы ЕГЭ/ДВИ и контакты факультетов уже нормализованы. Перечень направлений из плана приёма не означает полноту всех параметров каждого направления. Затем применить тот же процесс к остальным вузам.

Не переходить к вероятности поступления, новым фильтрам или редизайну: это Layer 02 и Layer 03.

## Фокус следующего ревью

- Совпадает ли adapter registry с каталогом после чистого checkout.
- Падает ли pipeline при искусственно удалённом или продублированном adapter.
- Создаётся ли новая версия при изменении provenance без изменения числового значения.
- Не объявлены ли неподтверждённые экзамены и числовые показатели verified.

## Блок 14 сентября: минимумы, стоимость, сроки и комиссии МГУ

- Нормализованы минимумы всех предметов ЕГЭ и ДВИ 85 групп, включая исключения мехмата, ВМК, экономики, филологии, ИСАА, ИИ и творческих испытаний. Для альтернатив сохраняется индивидуальный минимум каждого предмета.
- Подбор выбирает лучшую допустимую альтернативу, проверяет диапазон 0–100 и порог ДВИ; сравнение с историческим баллом блокируется при неизвестной/другой шкале. Тренд МГУ отключён.
- Добавлены 39 факультетских комиссий и отдельные источники 64 цен / 40 сроков. Числа относятся к точной конкурсной группе. Остальные поля pending_review.
- В карточках программ и раскрытых строках МГУ показаны минимумы, ДВИ, стоимость, срок и контакты с источниками. Исправлена читаемость блоков источников и полей ввода на мобильных ширинах; тема и навигация продукта сохранены.
- Условия общежития сверены по п. 14 правил МГУ 2026; сезонный адрес политологии обозначен как устаревший.
- Pipeline включает новые источники, контакты, сроки и индивидуальные минимумы в метрики. Неизвестная продолжительность стала nullable в Prisma; дробные годы преобразуются без потери месяцев. Миграция подготовлена; живая БД не менялась, импорт выполнен в dry-run.
- Существовавшие staged изменения сохранены. Layer 01 остаётся активным; полнота данных МГУ не объявляется завершённой.
- Подробная матрица 39 факультетов и ограничения источников: [MGU_DATA_COVERAGE.md](MGU_DATA_COVERAGE.md).

Проверки финальной версии и браузерная QA будут записаны после завершения.

## Блок 15 сентября: московский каталог организаций

- По запросу пользователя расширен справочник вузов без добавления новых направлений: 162 карточки (142 новых), прежние 185 программ сохранены. Основная таблица мониторинга 2025 покрыта полностью с учётом присоединённой к МГИМО Дипломатической академии; отдельно добавлены 12 записей вне основной таблицы.
- Каталог включает 77 негосударственных организаций и 3 филиала; МФТИ явно обозначен городом Долгопрудный. У каждой записи есть источник и период сведений; неизвестные адреса, общежития и ВУЦ не подменяются числом или false.
- Логотипы доступны для 67 вузов, у 95 отсутствуют. Новые локальные изображения проверены визуально, имеют точные исходные URL и SHA-256; белые знаки получают контрастный фон. Чужие партнёрские изображения исключены.
- Добавлены поиск, фильтр формы собственности, порционная загрузка и отдельные состояния страниц без программ. Импорт в БД выбирает вуз по адаптеру даже при отсутствии программ; проверен dry-run РосНОУ (1 вуз, 0 программ). БД не менялась.
- Полный реестр всех действующих лицензий не заявляется: Рособрнадзор не ответил, остаются непроверенные ведомственные организации. Список пробелов и границ: [MOSCOW_UNIVERSITY_COVERAGE.md](MOSCOW_UNIVERSITY_COVERAGE.md), `data-sources/moscow/coverage.json`.
- МГУ остаётся незавершённым по данным: 64 цены, 40 сроков, 39 факультетских комиссий; сохраняются 322/324 по математике и 40/42 для минимума ДВИ социологии. Матрица покрытия синхронизирована с уже внесёнными сведениями. Исследование остальных полей МГУ не продолжалось в этом блоке.
- Полный набор проверок и браузерная QA: результаты фиксируются ниже после запуска.

### Проверки блока московских вузов — 15 сентября

- Последовательно пройдены `pnpm format:check`, `pnpm lint` (без предупреждений), `pnpm typecheck`, `pnpm test` (248 тестов), `pnpm build`, `pnpm test:integration` (7 тестов) и `pnpm security:audit` (известных уязвимостей нет).
- `pnpm data:validate`, общий import dry-run (162 вуза, 185 программ), отдельный dry-run РосНОУ (1 вуз, 0 программ), генерация quality report — успешны. Все 162 university routes отдельно вернули HTTP 200 и профиль с заголовком. Рабочий и staged diff проходят `git diff --check`; прежние staged изменения сохранены, новых коммитов не создавалось.
- Браузер: production `http://127.0.0.1:3100`; проверены `/universities`, `/universities/rosnou`, `/universities/avr`, `/universities/mgu` на desktop 1440×1000 и mobile 390×844. Поиск РосНОУ/МГУ/Косыгина, пустой результат и сброс, фильтр частных вузов (77), подгрузка 24→48 карточек, переход и возврат в каталог, источники, отсутствие программ, неизвестный адрес и fallback логотипа работают. Возврат в каталог сбрасывает поисковый запрос.
- Исправлено найденное переполнение мобильной сетки ключевых фактов. Ширина документа совпадает с доступной шириной viewport; белый знак РосНОУ читается на тёмном фоне. Ошибок приложения в console не обнаружено. Неуспешный первоначальный локатор браузерной проверки исправлен; это не ошибка приложения.
- Сайт оставлен запущенным в фоне через `.next/standalone/server.js` на loopback-порту 3100, с копиями `public` и `.next/static` внутри standalone. Перед новой сборкой этот процесс нужно остановить: Windows блокирует удаление используемого каталога standalone (EBUSY). Ошибка промежуточной сборки устранена остановкой процесса, итоговые проверки пройдены.
- Данные остаются неполными: 95 логотипов и полная проверка действующих лицензий не закрыты. Успешные технические проверки не означают завершённость реестра или прежней задачи МГУ.

## Продолжение 15 сентября: сведения, лицензии и перечни направлений

Пользователь поручил выполнить все предложенные шаги: сверку действующих вузов, логотипы, карточки, остатки МГУ и направления остальных вузов. Работа остаётся в Layer 01; перехода к следующему слою и изменений живой БД нет.

- На сайте остаются 162 организации и 185 конкурсных групп; отдельно показаны 419 опубликованных направлений/профилей у 15 вузов. Исторические укрупнённые области образования добавлены для 150 организаций и явно помечены 2025 годом.
- Новая модель `profiles.json` хранит отдельные источники полей, даты и SHA-256. Подтверждены основные сведения 44 организаций, email 45, ссылки поступления 54 и общежития 25. Добавлены 28 датированных выписок; текущий статус реестра остаётся непроверенным.
- Логотипы: 79 доступны, 83 отсутствуют; исправлен чужой знак Мосспорта у МГУСиТ. Проверенные локальные копии приоритетны над старым внешним favicon. SVG не переформатируются, SHA проверяются тестами.
- МГУ: 72 цены и 56 сроков; новые факты получают собственный checkedAt/retrievedAt. Не разрешены 13 цен, 29 сроков и расхождения 322/324, 40/42.
- UI показывает юридические сведения и даты выписок, источники общежитий, поиск и подгрузку перечней направлений. Перечни не участвуют в подборе ЕГЭ и не увеличивают количество конкурсных групп. Таблицы статистики выпускников/приёма не превращаются в программы.
- Первичная валидация данных и 252 unit-теста прошли. Полные обязательные gates и браузерная проверка фиксируются после финального прогона ниже.
- Полный запрос по наполнению не объявляется завершённым: недоступные сайты/реестр и нерешённые источники описаны в `MOSCOW_UNIVERSITY_COVERAGE.md`. Существовавшие staged изменения сохранены; коммитов и PR в этом продолжении не создавалось.

### Проверки продолжения — 16 сентября 2026

- Финальная версия последовательно прошла `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (252 теста / 20 файлов), `pnpm build`, `pnpm test:integration` (8 тестов), `pnpm security:audit` (известных уязвимостей нет). Логи: `tmp/directory-final-*.log`. Предыдущая ошибка форматирования сгенерированного quality report устранена до финального прогона.
- Валидация данных, общий import dry-run (162 организации, 185 конкурсных программ) и генерация quality report прошли. Живая БД не менялась. Рабочий и staged diff проходят `git diff --check`; существовавший индекс сохранён.
- Production UI проверен на `http://localhost:3100`: `/universities`, `/universities/mgusit`, `/universities/ranepa`, `/universities/rosnou`, `/universities/mgu`, экраны 1440×1000 и 390×844. Горизонтального переполнения документа нет; длинные юридические названия, адреса и профили помещаются в карточках.
- Каталог: поиск, фильтр негосударственных вузов (77), подгрузка 24→48 и переходы между каталогом и карточками. Направления: поиск кода 49.03.02, поиск «Туризм», пустой результат, очистка с клавиатуры, поиск профиля «Веб-разработка», подгрузка 12→24. Перечень РАНХиГС содержит 243 записи, формы обучения показаны отдельно.
- Проверены раскрытие исторических областей и документов, дата выписки МГУСиТ 02.09.2025 и заметная оговорка о непроверенном текущем статусе, отсутствие программ у РосНОУ, загруженные локальные логотипы. Чужой знак Мосспорта заменён собственным знаком МГУСиТ; белая символика читается на синем фоне.
- МГУ: фильтр ФГУ даёт 4 группы, в раскрытом «Менеджменте» показаны 557 580 ₽ / год и 4 года со ссылками и датами; сброс возвращает 85 групп. Ошибок приложения в browser console и server stderr не обнаружено. Ошибка неоднозначного тестового локатора `status` исправлена в самой браузерной проверке.
- При подключении старая вкладка содержала страницу ошибки соединения; проверка выполнена в новой вкладке того же встроенного браузера. Итоговый каталог оставлен открытым. Временное изменение viewport сброшено.
- Сайт оставлен запущенным через `.next/standalone/server.js`, loopback-порт 3100, PID 21324 на момент записи. `public` и `.next/static` скопированы в standalone; логи `tmp/directory-site-out.log` и `tmp/directory-site-err.log`. Перед новой сборкой нужно проверить командную строку PID и остановить этот сервер из-за блокировок Windows.
- Полнота данных остаётся ограниченной: 83 отсутствующих логотипа, неподтверждённые текущие лицензии, неполные карточки и условия приёма, 13 цен и 29 сроков МГУ, два расхождения в источниках МГУ. Технические проверки не означают завершение этих исследований.

## Продолжение 17 сентября: проверенные дополнения и видимость пробелов

Слой 01, прежняя ветка; изменения касаются происхождения и полноты каталога. Следующий слой не начат.

- 3 официальных логотипа: МХПИ, Литературный институт, МГАХ. Всего 82 из 162; у 80 логотип отсутствует.
- 6 основных полей МГАХ подтверждены официальной страницей с датой и хешем снимка. 45 организаций с основными контактами, email у 46.
- МГУ: МШЭ — 750 000 ₽/год, ФНМ — 4 года; 73 цены и 57 сроков. Осталось 12 цен и 28 сроков; оба конфликта прежние. Индивидуальные checkedAt не заменены датой массового прохода.
- Видимость качества: количество полей и дата проверки, фильтр наличия сведений, раскрытие недостающих полей. Выписка по-прежнему не обозначает действующую лицензию. Новая регрессия исключает дату попытки проверки реестра из даты сверки полей.
- Исследовательские ограничения и случай Шанинки записаны в MOSCOW_UNIVERSITY_COVERAGE.md; текущий юридический статус не выведен из вторичной публикации.
- Итоговые технические и браузерные проверки записаны ниже. База не изменялась; существовавший индекс сохранён.

### Проверки 17 сентября

- Данные проходят validate, import dry-run (162 вуза, 185 конкурсных групп); отчёт качества обновлён. Полный упорядоченный прогон format:check → lint → typecheck → test (253 / 20 файлов) → build → test:integration (8) → security:audit прошёл. Логи `tmp/september17-final-*.log`; известных production-уязвимостей нет.
- Production UI `http://localhost:3100`: каталог, `/universities/balletacademy`, `/universities/mgu`; desktop 1440×1000 и mobile 390×844. Проверены совместный поиск и фильтры, 46 карточек с контактами, пересечение с негосударственными (28), 15 с перечнями, 28 с выписками, пустой результат и полный сброс, дозагрузка 24→48. Даты документа и проверки различимы. Сверены новые значения МШЭ и ФНМ, их ссылки и даты.
- Логотипы МХПИ, Литературного института и МГАХ загружаются. Мобильная карточка МГАХ сохраняет читаемость длинного юридического имени и контактов; раскрытие пробелов работает. Ширина документа совпадает с viewport, ошибок приложения в console и server stderr нет. Ожидание одного перехода истекло в тестовом инструменте; последующая проверка URL, содержимого и HTTP 200 подтвердила успешную загрузку.
- В ходе QA устранено склонение счётчика «1 полей»: используется «Собрано сведений: N». После этой текстовой правки все семь gates повторно прошли; финальная сборка проверена в браузере. Изменений БД и новых коммитов нет.
- Каталог оставлен открытым, временный viewport сброшен. Production-сервер работает в фоне на 127.0.0.1:3100, PID 6200 на момент записи; команда `node D:\STUDY\.next\standalone\server.js`. Логи: `tmp/september17-site-out.log`, `tmp/september17-site-err.log`. Перед следующей сборкой проверить PID и остановить именно этот процесс из-за блокировок Windows. Полнота реестра/программ не объявляется достигнутой.
