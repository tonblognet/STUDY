# ADR 0002: PostgreSQL and Prisma

Status: accepted.

PostgreSQL provides transactions, constraints, JSON for source-specific metadata, full-text/index options and managed backup choices. Prisma provides type-safe application access; migrations keep production changes reproducible. Provider data is normalized, while immutable source artifacts and metric versions preserve evidence.
